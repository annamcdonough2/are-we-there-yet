# ============================================================
# ENVIRONMENT VARIABLES TEMPLATE
# ============================================================
#
# Copy this file to .env.local and fill in your values:
#   cp .env.example .env.local
#
# NEVER commit .env.local to git! It contains secrets.
# ============================================================

# Mapbox API Token
# Get from: https://mapbox.com → Create access token
# Security: Domain-restrict this token in Mapbox dashboard
VITE_MAPBOX_TOKEN=pk.your_mapbox_token_here

# Anthropic API Key (for server-side only in production)
# Get from: https://console.anthropic.com → Create API key
# Security: This key should NOT have VITE_ prefix in production
ANTHROPIC_API_KEY=sk-ant-your_anthropic_key_here

# DEVELOPMENT ONLY - Browser-accessible Claude API
# Remove this line for production! Use the serverless function instead.
VITE_ANTHROPIC_API_KEY=sk-ant-your_anthropic_key_here

# OpenAI API Key (for text-to-speech)
# Get from: https://platform.openai.com → Create API key
# Security: Server-side only, used by /api/speak endpoint
OPENAI_API_KEY=sk-your_openai_key_here
